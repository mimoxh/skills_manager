# 计划：Skills 视图"无标签"筛选功能

## 概述

在标签筛选栏新增"无标签"虚拟按钮，支持快速找到没有任何用户标签的 skills/agents。应用于 SkillsView、AgentsView、SkillInstallDialog 三处。

## 设计决策

- **哨兵值方案**：使用 `"__untagged__"` 作为 `selectedTagFilters: string[]` 中的特殊值，无需新增 state 变量
- **互斥切换**：选择"无标签"时清空真实标签，选择真实标签时清空"无标签"
- **"清空标签筛选"按钮**：已有的 `setSelectedTagFilters([])` 自然清空两种状态

## 实施步骤

### 1. 创建新分支

```bash
git checkout -b feat/untagged-filter
```

### 2. 修改 `src/index.css`（~L473 后新增）

```css
.badge.skill-tag-filter.untagged {
  background: var(--surface-raised);
  border-color: var(--border);
  color: var(--text-tertiary);
  font-style: italic;
}
.badge.skill-tag-filter.untagged.selected {
  background: var(--accent);
  border-color: var(--accent);
  color: white;
  font-style: normal;
  font-weight: 700;
  box-shadow: 0 2px 8px rgba(196, 125, 46, 0.22);
}
```

### 3. 修改 `src/components/views/SkillsView.tsx`

**A. `toggleTagFilter`（L147-153）— 改写为互斥逻辑：**

```ts
function toggleTagFilter(tag: string) {
  setSelectedTagFilters((current) => {
    if (tag === "__untagged__") {
      return current.includes("__untagged__") ? [] : ["__untagged__"];
    }
    const withoutUntagged = current.filter((v) => v !== "__untagged__");
    const lower = tag.toLowerCase();
    return withoutUntagged.some((v) => v.toLowerCase() === lower)
      ? withoutUntagged.filter((v) => v.toLowerCase() !== lower)
      : [...withoutUntagged, tag];
  });
}
```

**B. `matchesTagFilters`（L436-440）— 增加哨兵判断：**

```ts
function matchesTagFilters(skill: GroupedSkill, selectedTags: string[]): boolean {
  if (!selectedTags.length) return true;
  if (selectedTags.includes("__untagged__")) {
    return !(skill.userTags ?? []).length;
  }
  const tags = new Set((skill.userTags ?? []).map((t) => t.toLowerCase()));
  return selectedTags.every((t) => tags.has(t.toLowerCase()));
}
```

**C. 标签筛选行 JSX（L270-292）— 条件改为 `skills.length > 0`，在 map 前插入"无标签"按钮：**

- 将 `{allUserTags.length > 0 && (` 改为 `{skills.length > 0 && (`
- 在 `{allUserTags.map(` 之前插入：

```tsx
<button
  className={`badge skill-tag-filter untagged${selectedTagFilters.includes("__untagged__") ? " selected" : ""}`}
  onClick={() => toggleTagFilter("__untagged__")}
  type="button"
>
  无标签
</button>
```

### 4. 修改 `src/components/views/AgentsView.tsx`

**A. `toggleTagFilter`（L103-109）— 同 SkillsView 的改写**

**B. `matchesAgentTagFilters`（L502-506）— 增加哨兵判断**

**C. 标签筛选行 JSX（L145-167）— 条件改为 `agents.length > 0`，插入"无标签"按钮**

### 5. 修改 `src/components/views/SkillInstallDialog.tsx`

**A. `toggleAgentTagFilter`（L96-102）— 同 SkillsView 的改写**

**B. `matchesAgentTagFilters`（L318-322）— 增加哨兵判断**

**C. 标签筛选行 JSX（L172-194）— 条件改为 `agents.length > 0`，插入"无标签"按钮**

### 6. 构建验证

```bash
npm run build        # TypeScript 编译检查
cargo test           # 后端测试（无后端改动，确认无回归）
npm run native:build # 完整构建
```

### 7. 更新 AGENT.md 文档

记录新增的"无标签"筛选功能。

## 验证方式

1. 打开 SkillsView，确认"无标签"按钮始终显示在标签筛选行
2. 点击"无标签"，确认只显示无标签的 skills
3. 点击一个真实标签，确认"无标签"被取消，显示拥有该标签的 skills
4. 点击"清空标签筛选"，确认两种状态都被清除
5. 对 AgentsView 和 SkillInstallDialog 重复以上验证
